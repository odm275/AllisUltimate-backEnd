# import * from './generated/prisma.graphql'

type Mutation {
    createBlog(title: String, image: String, body: String): Blog!
    signup(email: String!, password: String!, name: String!): User!

}
type Query {
    blogs: [Blog]!
}